//후위식 전위식 중위식을 알아야한다.
function solution(input) {
   let answer = 0;
   const stack = [];
   const split = Array.from(input);

   for (let i = 0; i < split.length; i++) {

   }

}

const data = solution("()(((()())(())()))(())");

